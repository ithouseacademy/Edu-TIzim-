{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "python manage.py collectstatic --noinput"
  },
  "deploy": {
    "startCommand": "python manage.py migrate --noinput && python manage.py load_fixture && python manage.py ensure_superuser && python manage.py set_telegram_webhook && gunicorn backend.wsgi --bind 0.0.0.0:8080",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}